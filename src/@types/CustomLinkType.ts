import { CSSProperties } from 'react'

interface CustomLinkType {
  link?: string
  sxStyle: CSSProperties
  text: string
  hasIcon: boolean
  fromWelcomeSection?: boolean
}

export default CustomLinkType
